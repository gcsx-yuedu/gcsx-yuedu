<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.Book_TypeMapper">
	<select id="queryType" parameterType="Integer" resultMap="Type">
		select * from book_type where book_id={#b_id}
	</select>
	<resultMap id="Type" type="Book_Type">
		<id property="id" column="id"/>
		<collection property="TypeList" ofType="BookType" column="type_id"
					select="mapper.BookTypeMapper.queryTypeById"
		/>
	</resultMap>
</mapper>