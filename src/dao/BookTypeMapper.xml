<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.Book_TypeMapper">
	<select id="queryTypeById" parameterType="Integer" resultType="BookType">
		select * from typez where t_id={#type_id}
	</select>

</mapper>